---
# ?�임?�페?�스 기본 mTLS ?�정 (STRICT)
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: default

spec:
  mtls:
    mode: STRICT
---
# Web ?�어???��? ?�래???�반 HTTP)???�신?�야 ?��?�?PERMISSIVE
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: web-peerauthentication

spec:
  selector:
    matchLabels:
      app: web
  mtls:
    mode: PERMISSIVE
---
# Web ?�어 ?�근 ?�용 (Ingress Gateway ??Web)
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: web-access-policy

spec:
  selector:
    matchLabels:
      app: web
  action: ALLOW
  rules:
  - from:
    - source:
        # ?�그?�스 게이?�웨?�의 ID�??�용 (?�반?�으�?istio-ingressgateway)
        namespaces: ["${NAMESPACE}", "istio-system"]
---
# WAS ?�어 ?�근 ?�용 (Web ??WAS)
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: was-access-policy

spec:
  selector:
    matchLabels:
      app: was
  action: ALLOW
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/${NAMESPACE}/sa/default"]
    to:
    - operation:
        ports: ["8080"]
---
# WAS ??DB ?�근�??�용?�는 AuthorizationPolicy
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: db-access-policy

spec:
  selector:
    matchLabels:
      app: db
  action: ALLOW
  rules:
  - from:
    - source:
        principals: ["cluster.local/ns/${NAMESPACE}/sa/default"]
    to:
    - operation:
        ports: ["3306"]
